[project]
name = "fmpapi"
version = "0.1.0"
description = "Flexible Client for the 'Financial Modeling Prep' API"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "polars>=1.0.0",
    "pytest>=8.3.4",
]
